export default function TsidkenuProductBrand() {
  // Product branding is rendered inside each responsive shell instead of as a
  // fixed viewport overlay. This avoids covering authentication/onboarding UI
  // and lets the wide TSIDKENU wordmark scale correctly on phones and laptops.
  return null;
}
