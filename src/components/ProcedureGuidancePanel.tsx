"use client";
import { useCallback,useEffect,useState } from "react";
import Link from "next/link";
import { BookOpenCheck, CheckCircle2, Gavel, Route } from "lucide-react";

type Payload={success:boolean;error?:string;matter?:any;procedureTracks?:any[];authorities?:any[];compliance?:any[]};
export default function ProcedureGuidancePanel({matterId}:{matterId:string}){const[data,setData]=useState<Payload|null>(null),[msg,setMsg]=useState("");const load=useCallback(async()=>{const r=await fetch(`/api/matters/${matterId}/procedure`,{cache:'no-store'});const d=await r.json();setData(d);if(!d.success)setMsg(d.error||'Unable to load procedure guidance')},[matterId]);useEffect(()=>{void load()},[load]);const track=data?.procedureTracks?.[0];const config=track?.configuration||{};const steps=Array.isArray(config.steps)?config.steps:[];const docs=Array.isArray(config.required_documents)?config.required_documents:[];const risks=Array.isArray(config.risks)?config.risks:[];return <section className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm md:p-6"><div className="flex flex-col gap-3 lg:flex-row lg:items-start lg:justify-between"><div><div className="flex items-center gap-2 text-[#0f5b49]"><Route className="h-5 w-5"/><span className="text-[10px] font-black uppercase tracking-[.2em]">Procedure & compliance intelligence</span></div><h2 className="mt-2 text-2xl font-semibold text-slate-950">{track?.label_en||'Matter procedure guidance'}</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Guidance is driven by the matter classification, firm procedure parameters, compliance controls and Law Bank sources. It supports professional judgment; it does not silently decide law.</p></div><Link href="/law-bank" className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-2 text-xs font-bold text-slate-600"><BookOpenCheck className="h-4 w-4"/>Open Law Bank</Link></div>{msg&&<div className="mt-4 rounded-xl bg-slate-50 px-4 py-3 text-sm text-slate-600">{msg}</div>}<div className="mt-5 grid gap-4 xl:grid-cols-3"><Box title="Procedure sequence" icon={<Route className="h-4 w-4"/>}>{steps.length?steps.map((s:any,i:number)=><div key={i} className="flex gap-3 border-b border-slate-100 py-2 last:border-0"><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-emerald-50 text-[10px] font-bold text-emerald-800">{i+1}</span><div><p className="text-sm font-semibold text-slate-900">{typeof s==='string'?s:s.title||`Step ${i+1}`}</p>{typeof s==='object'&&s.detail&&<p className="mt-1 text-xs leading-5 text-slate-500">{s.detail}</p>}</div></div>):<p className="text-sm text-slate-400">No configured procedure track matches yet. Configure one in Firm Studio; source authorities remain visible.</p>}</Box><Box title="Required documents & risks" icon={<CheckCircle2 className="h-4 w-4"/>}><p className="text-[10px] font-black uppercase tracking-[.12em] text-slate-400">Documents</p>{docs.map((x:any,i:number)=><p key={`d${i}`} className="mt-2 text-sm text-slate-600">• {typeof x==='string'?x:x.title}</p>)}{!docs.length&&<p className="mt-2 text-sm text-slate-400">No procedure-specific required documents configured.</p>}<p className="mt-4 text-[10px] font-black uppercase tracking-[.12em] text-slate-400">Risks</p>{risks.map((x:any,i:number)=><p key={`r${i}`} className="mt-2 text-sm text-slate-600">• {typeof x==='string'?x:x.title}</p>)}{!risks.length&&<p className="mt-2 text-sm text-slate-400">No procedure-specific risk notes configured.</p>}</Box><Box title="Supporting authorities" icon={<Gavel className="h-4 w-4"/>}>{(data?.authorities||[]).slice(0,8).map((a:any)=><div key={a.id} className="border-b border-slate-100 py-2 last:border-0"><div className="flex items-start justify-between gap-2"><p className="text-sm font-semibold text-slate-900">{a.title}</p><span className="rounded-full bg-slate-100 px-2 py-1 text-[9px] font-bold uppercase tracking-[.1em] text-slate-500">{a.verification_status}</span></div><p className="mt-1 text-xs text-slate-500">{[a.citation,a.authority_type,a.issuing_body].filter(Boolean).join(' · ')}</p></div>)}{!(data?.authorities||[]).length&&<p className="text-sm text-slate-400">No matching firm authority has been indexed yet.</p>}</Box></div>{(data?.compliance||[]).length>0&&<div className="mt-5 rounded-2xl border border-slate-100 p-4"><p className="text-sm font-semibold text-slate-950">Live compliance controls</p><div className="mt-3 grid gap-3 md:grid-cols-2">{(data?.compliance||[]).map((c:any)=><div key={c.id} className="rounded-xl bg-slate-50 p-3"><div className="flex items-center justify-between"><p className="text-sm font-semibold">{c.title}</p><span className="text-xs text-slate-500">{c.overall_status}</span></div>{(c.compliance_checklist_items||[]).map((i:any)=><p key={i.id} className="mt-2 text-xs text-slate-600">• {i.label} · {i.status}</p>)}</div>)}</div></div>}</section>}
function Box({title,icon,children}:{title:string;icon:React.ReactNode;children:React.ReactNode}){return <div className="rounded-2xl border border-slate-100 p-4"><div className="flex items-center gap-2 text-sm font-semibold text-slate-950">{icon}{title}</div><div className="mt-3">{children}</div></div>}
